{
  "name": "sql-practice",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "knex migrate:latest && knex seed:run && node ./src/app.js",
    "start-dev": "knex migrate:rollback && knex migrate:latest && knex seed:run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "pg": "^8.12.0"
  }
}
